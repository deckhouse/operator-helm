{{- $nelmSourceControllerTag := "v0.1.5" }}
---
image: {{ .ModuleNamePrefix }}{{ .ImageName }}-src-artifact
final: false
fromImage: builder/src
git:
  - url: {{ .SOURCE_REPO }}/werf/nelm-source-controller.git
    tag: {{ $nelmSourceControllerTag }}
    add: /
    to: /src/nelm-source-controller
---
image: {{ .ModuleNamePrefix }}{{ .ImageName }}-artifact
final: false
fromImage: builder/golang
import:
- image: {{ .ModuleNamePrefix }}{{ .ImageName }}-src-artifact
  add: /src
  to: /src
  before: install
packages:
  - type: go-mod
    workdir: /src/nelm-source-controller
    env:
      GOPROXY: "{{ .GOPROXY }}"
shell:
  install:
    - cd /src/nelm-source-controller
    - |
      export GOOS=linux
      export GOARCH=amd64
      export CGO_ENABLED=0
      {{- include "image-build.build" (set $ "BuildCommand" `go build -mod=readonly -trimpath -a -o source-controller main.go`) | indent 6 }}
---
image: {{ .ModuleNamePrefix }}{{ .ImageName }}
fromImage: base/distroless
import:
- image: {{ .ModuleNamePrefix }}{{ .ImageName }}-artifact
  add: /src/nelm-source-controller/source-controller
  to: /usr/bin/source-controller
  before: install
imageSpec:
  config:
    entrypoint: ["/usr/bin/source-controller"]
