---
image: {{ .ModuleNamePrefix }}{{ .ImageName }}-src-artifact
final: false
fromImage: builder/distroless
git:
- add: {{ .ModuleDir }}/api
  to: /src/api
- add: {{ .ModuleDir }}/images/{{ .ImageName }}
  to: /src/images/{{ .ImageName }}-artifact
---
image: {{ .ModuleNamePrefix }}{{ .ImageName }}-artifact
final: false
fromImage: builder/golang
import:
- image: {{ .ModuleNamePrefix }}{{ .ImageName }}-src-artifact
  add: /src
  to: /src
  before: install
mount:
  - fromPath: ~/go-pkg-cache
    to: /go/pkg
packages:
  - type: go-mod
    workdir: /src/images/{{ .ImageName }}-artifact
    env:
      GOPROXY: "{{ .GOPROXY }}"
shell:
  install:
  - cd /src/images/{{ .ImageName }}-artifact
  - mkdir /out
  - export GOOS=linux
  - export GOARCH=amd64
  - export CGO_ENABLED=0
  - |
    echo "Build chart-values-controller binary"
    {{- $_ := set $ "ProjectName" (list $.ImageName "chart-values-controller" | join "/") }}

    {{-   $buildCommand := printf "go build -mod=readonly -ldflags=\"-s -w\" -tags %s -v -a -o /out/chart-values-controller ./cmd/chart-values-controller" .MODULE_EDITION -}}
    {{-   include "image-build.build" (set $ "BuildCommand" $buildCommand) | nindent 4 }}
---
image: {{ .ModuleNamePrefix }}{{ .ImageName }}
fromImage: base/distroless
git:
  {{- include "image mount points" . }}
import:
- image:  {{ .ModuleNamePrefix }}{{ .ImageName }}-artifact
  add: /out/chart-values-controller
  to: /app/chart-values-controller
  after: install
imageSpec:
  config:
    workingDir: "/app"
    entrypoint: ["/app/chart-values-controller"]
