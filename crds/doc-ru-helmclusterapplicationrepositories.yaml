---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: helmclusterapplicationrepositories.helm.deckhouse.io
spec:
  versions:
    - name: v1alpha1
      schema:
        openAPIV3Schema:
          description: HelmClusterApplicationRepository представляет собой кластерный Helm- или OCI-совместимый репозиторий, содержащий Helm-чарты, на которые могут ссылаться ресурсы HelmApplication из любого пространства имён.
          properties:
            spec:
              properties:
                auth:
                  description: Учётные данные для аутентификации в репозитории.
                  properties:
                    password:
                      description: Пароль для аутентификации в репозитории.
                    username:
                      description: Имя пользователя для аутентификации в репозитории.
                caCertificate:
                  description: CA-сертификат в формате PEM для проверки TLS.
                insecureSkipVerify:
                  description: Выключить проверку TLS-сертификата.
                url:
                  description: |
                    URL Helm-репозитория.

                    Поддерживаются протоколы `http(s)://` и `oci://`.
            status:
              properties:
                conditions:
                  description: |
                    Условия отражают последние наблюдения за состоянием репозитория.

                    `Ready` сообщает, пригоден ли репозиторий: вспомогательные ресурсы на месте, внутренний объект источника исправен, и репозиторий ответил на чтение каталога на текущей спецификации. Транзиентная ошибка чтения не переводит `Ready` в `False`.

                    `Synced` сообщает, актуален ли каталог чартов.

                    `Reconciling` и `Stalled` следуют соглашению kstatus: они присутствуют, только когда применимы. `Reconciling` означает, что работа выполняется или запланирован повтор; `Stalled` — что репозиторий не восстановится без вмешательства. Пока выполняется синхронизация, `Reconciling` имеет причину `Synchronization`, либо `ForceReconcile`, если проход был запрошен аннотацией принудительной реконсиляции.
                observedGeneration:
                  description: Поколение ресурса, обработанное контроллером последним.
                lastSuccessfulSyncTime:
                  description: Время последнего успешного приведения каталога чартов в актуальное состояние.
                nextSyncTime:
                  description: Запланированное время следующей попытки синхронизации.
                lastForceReconcileTime:
                  description: |
                    Время обработки последнего запроса принудительной реконсиляции. Фиксирует, что запрос был обработан, а не что он завершился успешно — результат отражают `Ready` и `Synced`.
                consecutiveFetchFailures:
                  description: Число подряд идущих неудачных обращений к репозиторию. Определяет задержку повтора и обнуляется при первом успехе.
